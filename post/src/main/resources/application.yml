spring:
  application:
    name: post
  profiles:
    active: local
  config:
    import: "optional:configserver:http://localhost:8888"
  cloud:
    config:
      fail-fast: true
      username: user
      password: nimadirxavfsizlik